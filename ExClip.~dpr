program ExClip;

uses
//  fastmm4,
  Forms,
  ModMain in 'ModMain.pas' {DataModule1: TDataModule},
  FrText in 'FrText.pas' {TextForm};

{$R *.res}

begin
  Application.Initialize;
  Application.Title := 'ExClip';
  Application.CreateForm(TDataModule1, DataModule1);
  Application.Run;
end.
